#include <stdio.h>
int main(void)
{
    // declares i and j as variables (integer)
    int i, j;

    // declares x and y as variables (float)
    float x,y;

    // assigns values according to data types
    i = 10;
    j = 20;
    x = 43.2892f;
    y = 5527.0f;

    // prints the values of the variables using conversion specifications
    printf("i = %d, j = %d, x = %f, y= %f\n", i, j, x, y);

    return 0;
}